name: thai_pid_validator
description: a Flutter library for validating checksum of Thai Personal Identification (PID)
version: 0.0.1
# homepage:

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:

  